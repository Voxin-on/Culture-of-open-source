[project]
name = "order-service"
version = "0.0.1"
description = "App for serving user's orders"
authors = [
    {name = "Voxin",email = "alexlogvinenko2007@gmail.com"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi (>=0.136.1,<0.137.0)",
    "unicorn[standart] (>=2.1.4,<3.0.0)",
    "httpx (>=0.28.1,<0.29.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
